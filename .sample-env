COPY ALL CONTENT BELOW THIS LINE TO YOUR OWN .env file
in the root of this repository.

Remove any square bracket, they are just there for quick information

**ADDING NEW VARIABLES TO THIS FILE
    - When adding new variables, also include them in 
      the utils/checkEnvFile.js. Append variables in the
      criticalEnvVariables list.

\/ COPY BELOW THIS LINE TO YOUR .env FILE \/
---------------------------------------------------------

HOST=[host of the machine, usually `localhost`]
PORT=[port that the api will be hosted on, usually `3000`]
DB_CONNECT="uri-of-mongodb"
TOKEN_SECRET="can be any string"
LOCAL_UPLOAD_PATH=[use "uploads/" as default]