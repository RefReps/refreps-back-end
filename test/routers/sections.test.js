const router = require('../../routers/sections')

describe('GET path~/', () => {
	it('should get an array of documents of sections', (done) => {
		done()
	})
})

describe('POST path~/', () => {
	it('should post a new section', (done) => {
		done()
	})
})

describe('GET path~/:sectionId', () => {
	it('should get a specific section document', (done) => {
		done()
	})
})

describe('PUT path~/:sectionId', () => {
	it('should update a specific section document', (done) => {
		done()
	})
})

describe('DELETE path~/:sectionId', () => {
	it('should delete a specific section document', (done) => {
		done()
	})
})
