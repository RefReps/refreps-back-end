const router = require('../../routers/videos')

describe('GET path~/', () => {
	it('should get an array of documents of videos', (done) => {
		done()
	})
})

describe('POST path~/', () => {
	it('should post a new video', (done) => {
		done()
	})
})

describe('GET path~/:videoId', () => {
	it('should get a specific section video file', (done) => {
		done()
	})
})

describe('DELETE path~/:videoId', () => {
	it('should delete a specific video document and upload dir', (done) => {
		done()
	})
})
