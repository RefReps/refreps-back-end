const router = require('../../routers/contents')

describe('GET path~/', () => {
	it('should get an array of documents of contents', (done) => {
		done()
	})
})

describe('POST path~/', () => {
	it('should post a new content', (done) => {
		done()
	})
})

describe('GET path~/:contentId', () => {
	it('should get a specific content document', (done) => {
		done()
	})
})

describe('PUT path~/:contentId', () => {
	it('should update a specific content document', (done) => {
		done()
	})
})

describe('DELETE path~/:contentId', () => {
	it('should delete a specific content document', (done) => {
		done()
	})
})
