const router = require('../../routers/modules')

describe('GET path~/', () => {
	it('should get an array of documents of modules', (done) => {
		done()
	})
})

describe('POST path~/', () => {
	it('should post a new module', (done) => {
		done()
	})
})

describe('GET path~/:moduleId', () => {
	it('should get a specific module document', (done) => {
		done()
	})
})

describe('PUT path~/:moduleId', () => {
	it('should update a specific module document', (done) => {
		done()
	})
})

describe('DELETE path~/:moduleId', () => {
	it('should delete a specific module document', (done) => {
		done()
	})
})
