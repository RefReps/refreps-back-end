const { userInfo, userSchema } = require('../../schemas/user')

describe('userInfo', () => {
	it('should contain correct key-values', (done) => {
		done()
	})
})

describe('userSchema', () => {
	it('should build correctly into a mongo model', (done) => {
		done()
	})
})
